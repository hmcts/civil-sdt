apply plugin: 'info.solidsoft.pitest'

group 'uk.gov.hmcts.reform'
version '0.0.1'

dependencyCheck {
  suppressionFile = "${rootDir}/config/owasp/suppressions.xml"
}

jar {
  enabled = true
  baseName = 'civil-sdt-utils'
}

bootJar {
    enabled = false
}
