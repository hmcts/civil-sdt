plugins {
    id 'java'
}

group 'uk.gov.hmcts.reform'
version '0.0.1'

repositories {
    mavenCentral()
}

dependencyCheck {
  suppressionFile = "${rootDir}/config/owasp/suppressions.xml"
}

bootJar {
    enabled = false
}
