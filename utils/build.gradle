jar {
  enabled = true
  archiveBaseName = 'civil-sdt-utils'
}

dependencyCheck {
    suppressionFile = "${rootDir}/config/owasp/suppressions.xml"
}

bootJar {
    enabled = false
}

test {
    useJUnitPlatform()
}

pitest {
    targetClasses = ['uk.gov.moj.sdt.utils.*']
    targetTests = ['uk.gov.moj.sdt.utils.*']
}
