apply plugin: 'info.solidsoft.pitest'

dependencyCheck {
    suppressionFile = "${rootDir}/config/owasp/suppressions.xml"
}

jar {
    enabled = true
    baseName = 'civil-sdt-dao'
}

bootJar {
    enabled = false
}
